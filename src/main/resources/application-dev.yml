spring:
  datasource:
    url: jdbc:postgresql://db:5432/dothelist_db?useSSL=false
    username: db
    password: db
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
